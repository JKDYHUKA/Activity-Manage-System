<template>
  <div v-if="isRetrieve">
    <register/>
  </div>
  <button class="upward-shift" @click="changetype" v-if="isRetrieve">找回密码</button>
  <div v-if="!isRetrieve">
    <resetpwd/>
  </div>
  <button class="upward-shift" @click="changetype" v-if="!isRetrieve">返回</button>
</template>

<script>
import register from './components/register.vue'
import resetpwd from './components/reset_pwd.vue'

export default {
  name: 'App',
  components: {
    register,
    resetpwd
  },
  data(){
			return {
				isRetrieve:true,

			}
	},
  methods:{
    changetype(){
      this.isRetrieve=!this.isRetrieve;
    }

  }
}
</script>

<style>
#app {
  font-family: Avenir, Helvetica, Arial, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-align: center;
  color: #2c3e50;
  margin-top: 60px;
}
.upward-shift {
  position: relative;
  top: -188px;
  left:11px;
  transform: translate(0, -100px);
}
</style>
